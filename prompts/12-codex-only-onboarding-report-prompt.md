This prompt is for **Codex execution only**.

Do **not** action this prompt with Gemini, Claude, ChatGPT, or any other agent. The onboarding report must be generated by Codex because it needs to reflect repository structure, implementation details, HPC execution paths, and the current state of tracked versus documented outputs with engineering-level precision.

## Task

Generate a comprehensive onboarding report for this repository so that another agent can be shown the report and quickly understand how the repo works, what the active workstreams are, how jobs are run on Hyperion, where outputs live, and what the current gaps / inconsistencies are.

Repository root: `/home/daniel/git/vidiq-hpc`

## Primary Deliverable

Create a new markdown report at a sensible location in the repo, using a clear name such as:

- `reports/codex-onboarding-report.md`

Use that exact path unless there is a strong repo-consistency reason to choose a better one.

## Scope

The report must help a new technical agent understand:

1. the repository purpose
2. the directory layout
3. the main experiment families
4. the current HPC execution model
5. the current source-of-truth files for text, Qwen, parity, and image work
6. what is implemented versus what is only planned
7. the main known inconsistencies or unfinished areas
8. how to safely add, run, and push new experiment outputs

## Read First

At minimum, inspect:

- `README.md`
- `experiments.md`
- `work-diary.md`
- `experiments/README.md`
- `experiments/text/multiclass/dair-ai-emotion/README.md`
- `experiments/text_model/README.md`
- `hpc/`
- `configs/`
- `scripts/`
- `src/`
- relevant reports and prompts where needed

You must also inspect the actual filesystem state, not just the documentation, because this repo has had cases where docs described outputs that were not present in the checkout.

## Required Report Structure

The onboarding report must contain these sections:

### 1. Repository Overview

- what the project is for
- what kinds of experiments it contains
- what the current major workstreams are

### 2. Directory Map

- concise explanation of the important top-level directories
- what belongs in each one
- which ones are code, configs, reports, prompts, outputs, or HPC wrappers

### 3. Experiment Families

Cover at least:

- binary text embedding experiments
- multiclass `dair-ai/emotion` experiments
- Qwen fine-tuning pipeline
- Qwen bridge / Phase 3 analysis
- Qwen parity workflow
- `embeddings_field` geometry analysis
- image embedding framework

For each:

- purpose
- main code entrypoints
- main config files
- main output locations
- current state: active, stable, partial, or planned

### 4. HPC / Hyperion Workflow

- how SLURM jobs are organized
- which batch files are important
- where the shared venv is expected
- where large data / artifacts should live
- how results are normally pushed back

### 5. Current Source of Truth

This section is critical.

Identify which files should currently be trusted as the best source of truth for:

- experiment inventory
- current text-model behavior
- multiclass Qwen behavior
- Qwen parity status
- image framework status
- operational job commands

### 6. Known Gaps / Inconsistencies

Be explicit about things such as:

- docs that may be ahead of actual committed outputs
- implemented code that still lacks produced artifacts
- frameworks that are extraction-only rather than train/eval complete
- ignored outputs versus tracked outputs
- any current mismatch between intended and actual workflow

### 7. Safe Working Rules For Future Agents

Explain:

- where to look first before making changes
- how to verify whether an output really exists
- how to distinguish tracked outputs from ignored heavy artifacts
- how to avoid misleading README updates
- how to decide when a work-diary update is warranted

### 8. Recommended Next Actions

Give a short prioritized list of the most important next technical tasks.

## Requirements

- This report must be based on actual repo inspection, not assumptions.
- Prefer concrete file and directory references over vague descriptions.
- Distinguish clearly between:
  - implemented code
  - tracked outputs
  - documented intent
  - planned future work
- If documentation and filesystem state disagree, say so explicitly.
- Keep it concise enough to be practical, but complete enough that another agent can onboard from it without re-discovering the repo from scratch.

## Important Restriction

Do not write this report as a generic summary for any LLM.

Write it as a technically precise repository onboarding report generated by Codex from direct inspection of the codebase and repo state.

## Final Output

When finished:

- create the onboarding report markdown file
- state the exact path created
- summarize the highest-priority gaps that the next agent should understand immediately
